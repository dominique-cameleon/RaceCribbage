{
  "name": "racecribbage",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Moteur de jeu RaceCribbage — cribbage regulier + mode Qualif solo. Zero dependance, ES modules natifs.",
  "scripts": {
    "test": "node --test \"tests/**/*.test.js\""
  },
  "engines": {
    "node": ">=18"
  }
}
